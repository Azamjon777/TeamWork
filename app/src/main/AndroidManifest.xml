<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeamWork"
        tools:targetApi="31" >
        <activity
            android:name=".SafetyContainerActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activities.Login1Activity"
            android:exported="false"
            android:label="@string/title_activity_login1"
            android:theme="@style/Theme.TeamWork.NoActionBar" />
        <activity
            android:name=".presentation.activities.TaxiContainerActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activities.TaxiActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.activities.ProsActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>